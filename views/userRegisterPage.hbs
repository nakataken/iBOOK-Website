<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/userRegister.css">
    <link rel="shortcut icon" href="images/new/eb4.png" type="image/x-icon" />
    <script src="https://kit.fontawesome.com/85f9dbedb7.js" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

</head>

<body>
    <!--Navigation Bar-->

    {{> categoryHeader}}
    <main>
        <div class="container">
            <div class="row px-3">
                <div class="col-lg-12 col-xl-11 card flex-row mx-auto px-0">
                    <div class="card-body">
                        <h4 class="title text-left mt-4">
                            Sign Up
                        </h4>
                        <h6 class="title text-left mt-n4">
                            Let's create your account
                        </h6>
                        <form class="register form-box pl-1 pr-2" action="/auth/userRegisterPage" method="POST">
                            <div class="form-input mb-3">
                           <span><i class="far fa-user"></i></span>
                                <input class="mb-1" id="userUsername" type="username" name="userUsername"
                                    placeholder="Username" tabindex="10" required onsubmit="/auth/userRegisterPage"
                                    minlength="1" maxlength="10">
                                {{#if usernameMessage}}
                                <p class="text-danger font-weight-bold small mt-0 ml-2 p-0 mb-1">{{usernameMessage}}</p>
                                {{/if}}
                            </div>
                            <div class="form-input">
                            <span><i class="far fa-envelope"></i></span>    
                                <input type="email" name="userEmail" placeholder="Email Address" tabindex="10"
                                    maxlength="50" required>
                                {{#if emailMessage}}
                                <p class="text-danger font-weight-bold small mt-0 ml-2 p-0 mb-1">{{emailMessage}}</p>
                                {{/if}}
                            </div>
                            <div class="form-input">
                            <span><i class="medium material-icons">lock_outline</i></span>
                                <input type="password" id="userPassword" name="userPassword" minlength="6"
                                    maxlength="10" placeholder="Password" tabindex="10" required>
                                {{#if passwordMessage}}
                                <p class="text-danger font-weight-bold small mt-0 ml-2 p-0 mb-1">{{passwordMessage}}</p>
                                {{/if}}
                            </div>
                            <div class="form-input">
                             <span><i class="medium material-icons">lock_outline</i></span>
                                <input type="password" name="userPasswordConfirm" placeholder="Confirm Password"
                                    minlength="6" maxlength="10"tabindex="10" required>
                            </div>
                            <div class="mb-3">
                                <button type="submit" class="btn btn-block text-uppercase">
                                    Register
                                </button>
                            </div>
                            {{#if message}}
                            <p class="alert alert-success text-center p-1">{{message}}</p>
                            {{/if}}
                            <hr class="my-4">
                            <div class="text-center mb-2">
                                Already have an account?
                                <a href="/userLoginPage" class="login-link">Sign in here</a>
                            </div>
                        </form>
                    </div>
                    
                       <div class="img-left d-none d-md-flex">
                            <h5>Join our</h5>
                            <h4 class="signup text-left">
                            E-bookada Community! 
                            </h4> 
                            <img class = "sign-img" src="/images/signup.jpeg">
                           
                    </div> 

                </div>

            </div>
        </div>
    </main>

    

    

    <script>
        $(document).ready(function () {
            $('[data-toggle=search-form]').click(function () {
                $('.search-form-wrapper').toggleClass('open');
                $('.search-form-wrapper .search').focus();
                $('html').toggleClass('search-form-open');
            });
            $('[data-toggle=search-form-close]').click(function () {
                $('.search-form-wrapper').removeClass('open');
                $('html').removeClass('search-form-open');
            });
            $('.search-form-wrapper .search').keypress(function (event) {
                if ($(this).val() == "Search") $(this).val("");
            });
        });
    </script>

    <script>
        $(document).ready(function () {
            // Add smooth scrolling to all links
            $("a").on('click', function (event) {

                // Make sure this.hash has a value before overriding default behavior
                if (this.hash !== "") {
                    // Prevent default anchor click behavior
                    event.preventDefault();

                    // Store hash
                    var hash = this.hash;

                    // Using jQuery's animate() method to add smooth page scroll
                    // The optional number (800) specifies the number of milliseconds it takes to scroll to the specified area
                    $('html, body').animate({
                        scrollTop: $(hash).offset().top
                    }, 800, function () {

                        // Add hash (#) to URL when done scrolling (default click behavior)
                        window.location.hash = hash;
                    });
                } // End if
            });
        });
    </script>



    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>



</body>

</html>